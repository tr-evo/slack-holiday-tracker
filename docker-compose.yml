services:
  holiday-bot:
    build: .
    container_name: holiday-tracker-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot-data:/app/data
    environment:
      - DB_PATH=/app/data/holidays.db

volumes:
  bot-data:
